you found the workspace. signal steady.

this is the sandbox — a small directory that belongs to your session.
anything you write here will be erased when you disconnect or go idle
for five minutes. that's not a threat. it's just how things are.

poke around. try `ls -la`. check the `notes/` and `relics/` folders.
the `bin/` directory has some small tools.

there is no wrong way.
